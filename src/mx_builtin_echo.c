#include "../inc/ush.h"

void mx_builtin_echo(char **params, t_flags_echo *flags) {
    mx_print_strarr(params, "\n");
    printf("%s ", "\n");
    if (flags->using_n) {
        
    }
    else if (flags->using_e) {
        
    }
    else if (flags->using_E) {
        
    }
}
